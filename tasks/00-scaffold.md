# Task 00 — Repo scaffold + CI baseline

## Goal
Create the initial repository structure, task tracking workflow, and a minimal CI pipeline to enforce formatting + tests.

## Scope
- Add task framework (`tasks/`) with STATUS tracker.
- Add basic Makefile with placeholders for build/test/lint.
- Add minimal GitHub Actions CI that runs on PRs.

## Acceptance criteria
- CI runs on PR and passes.
- Repo has clear structure for subsequent tasks.

## Notes
- Real implementation work starts in Task 01+.
