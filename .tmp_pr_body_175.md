## Summary
- add responsiveness preflights for the real-mount verify mountpoint and backstore paths
- abort early with reboot guidance when the host appears to be in the macFUSE wedge state
- record issue #175 as the active in-progress verification task

## Context
This addresses issue #175, where non-interactive real-mount verification on macOS can encounter a macFUSE stuck-mount state and wedge `umount`, `ls`, or `mkdir` calls. The helper script now checks those paths with short timeouts before risky operations so it fails fast with actionable diagnostics instead of piling up more stuck processes.

Closes #175

## Test Plan
- bash -n scripts/real-mount-verify.sh
